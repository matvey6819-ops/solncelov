<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
<title>солнцелов</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Unbounded:wght@600;800&family=Manrope:wght@500;700&display=swap" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script>
/* ===== НАСТРОЙКИ, вставь сюда данные своего проекта supabase ===== */
var CONFIG = {
  url: 'https://agnjpougjohokauuqcfl.supabase.co',
  key: 'sb_publishable_C-zF2VFkYyGD9RF5TBubpg_yrK0l4Cc'
};
/* ================================================================= */
</script>
<style>
:root{
  --bg:#CDE7FF; --bg2:#EEF7FF; --ink:#1C1A3D; --ink2:#5B5985;
  --sun:#FFC531; --sun2:#FF8A1F; --sun-ink:#7A3E00;
  --card:rgba(255,255,255,.72); --line:rgba(28,26,61,.12); --ray:rgba(255,197,49,.38);
  --r0:#8A8AA3; --r1:#2F7BD6; --r2:#8E4BD8; --r3:#D48A00;
  --ok:#1E8A4B; --bad:#C43A3A; --me:rgba(255,197,49,.22);
}
@media (prefers-color-scheme: dark){
  :root{
    --bg:#15183D; --bg2:#0E1030; --ink:#FFF3DC; --ink2:#B5B2D8;
    --card:rgba(255,255,255,.07); --line:rgba(255,243,220,.14); --ray:rgba(255,197,49,.2);
    --r0:#A6A6BF; --r1:#6FA8F0; --r2:#B98CF0; --r3:#FFC531; --ok:#4FCB84; --bad:#F07070; --me:rgba(255,197,49,.14);
  }
}
*{box-sizing:border-box}
html,body{margin:0}
body{
  background:linear-gradient(180deg,var(--bg) 0%,var(--bg2) 60%,var(--bg2) 100%);
  color:var(--ink);
  font-family:"Manrope",system-ui,-apple-system,"Segoe UI",Roboto,sans-serif;
  font-weight:500; font-size:16px; line-height:1.45;
  min-height:100vh; padding:0 0 64px;
}
h1,h2,.disp{font-family:"Unbounded","Manrope",system-ui,sans-serif; font-weight:800; letter-spacing:-.01em; margin:0}
.wrap{max-width:520px; margin:0 auto; padding:0 18px}
img{max-width:100%}
button{font:inherit; color:inherit; cursor:pointer; border:0; background:none; -webkit-tap-highlight-color:transparent}
button:focus-visible,input:focus-visible,textarea:focus-visible{outline:3px solid var(--sun2); outline-offset:3px}
input[type=text]{width:100%; font:inherit; padding:12px 14px; border-radius:14px; border:1px solid var(--line); background:var(--card); color:var(--ink)}

.top{display:flex; align-items:flex-end; justify-content:space-between; padding:22px 0 6px; gap:12px}
.top h1{font-size:26px; line-height:1}
.top .sub{color:var(--ink2); font-size:13px; margin-top:6px}
.score{text-align:right}
.score .n{font-family:"Unbounded",sans-serif; font-weight:800; font-size:28px; line-height:1}
.score .l{font-size:12px; color:var(--ink2)}
.lvl{margin:10px 0 0; font-size:13px; color:var(--ink2); display:flex; align-items:center; gap:10px}
.lvl .bar{flex:1; height:6px; border-radius:6px; background:var(--line); overflow:hidden}
.lvl .bar i{display:block; height:100%; background:linear-gradient(90deg,var(--sun),var(--sun2)); border-radius:6px; transition:width .5s ease}
.status{font-size:12px; color:var(--ink2); margin-top:6px}

.hero{position:relative; height:320px; display:grid; place-items:center; margin:8px 0 6px; overflow:hidden}
.rays{position:absolute; width:520px; height:520px; border-radius:50%;
  background:repeating-conic-gradient(from 0deg,var(--ray) 0 6deg,transparent 6deg 15deg);
  -webkit-mask:radial-gradient(circle,transparent 118px,#000 122px,#000 55%,transparent 62%);
  mask:radial-gradient(circle,transparent 118px,#000 122px,#000 55%,transparent 62%);
  animation:spin 90s linear infinite; pointer-events:none}
@keyframes spin{to{transform:rotate(360deg)}}
.sun{position:relative; width:230px; height:230px; border-radius:50%;
  background:radial-gradient(circle at 36% 32%,#FFE79A 0%,var(--sun) 52%,var(--sun2) 100%);
  box-shadow:0 0 0 14px var(--ray),0 24px 70px rgba(255,138,31,.45);
  color:var(--sun-ink); display:grid; place-items:center; text-align:center; transition:transform .15s ease}
.sun:active{transform:scale(.96)}
.sun .disp{font-size:22px}
.sun small{display:block; font-size:12px; font-weight:700; opacity:.75; margin-top:6px}
.sun.wobble{animation:wob 1.4s cubic-bezier(.36,.07,.19,.97) both}
@keyframes wob{0%,100%{transform:rotate(0)} 12%{transform:rotate(-14deg) translateX(-8px)} 25%{transform:rotate(12deg) translateX(8px)} 38%{transform:rotate(-10deg)} 50%{transform:rotate(9deg)} 62%{transform:rotate(-6deg)} 75%{transform:rotate(5deg)} 88%{transform:rotate(-2deg)}}
.sun.pop{animation:pop .5s ease both}
@keyframes pop{0%{transform:scale(1)} 40%{transform:scale(1.12)} 100%{transform:scale(1)}}
.sun.shake{animation:shk .5s ease both}
@keyframes shk{0%,100%{transform:translateX(0)} 25%{transform:translateX(-10px)} 75%{transform:translateX(10px)}}
.sun.night{filter:saturate(.6) brightness(.85)}
input[type=file]{position:absolute; width:1px; height:1px; opacity:0; pointer-events:none}

.today{background:var(--card); border:1px solid var(--line); border-radius:22px; padding:14px 16px; margin-top:6px}
.today .m{display:flex; align-items:baseline; gap:10px}
.today .m .disp{font-size:30px; color:var(--sun2)}
.today .m span{color:var(--ink2); font-size:13px}
.today .g{display:grid; grid-template-columns:1fr 1fr 1fr; gap:8px; margin-top:10px; font-size:13px}
.today .g b{display:block; font-weight:700; font-size:15px}
.today .g span{color:var(--ink2); font-size:12px}
.today .why{margin-top:10px; font-size:13px; color:var(--ink2); line-height:1.4}
.today .loc{margin-top:10px; display:flex; justify-content:space-between; align-items:center; font-size:13px; color:var(--ink2); gap:8px}
.today .loc button{color:var(--sun2); font-weight:700; font-size:13px; padding:4px 0}

section{margin-top:26px}
section h2{font-size:17px; margin-bottom:12px; display:flex; justify-content:space-between; align-items:baseline}
section h2 span{font-family:"Manrope",sans-serif; font-weight:500; font-size:13px; color:var(--ink2)}

.tabs{display:flex; gap:6px; margin-bottom:10px}
.tabs button{padding:8px 14px; border-radius:20px; border:1px solid var(--line); font-size:13px; font-weight:700; color:var(--ink2)}
.tabs button.on{background:var(--ink); color:var(--bg2); border-color:var(--ink)}
.board{background:var(--card); border:1px solid var(--line); border-radius:18px; overflow:hidden}
.board .r{display:grid; grid-template-columns:28px 1fr auto; gap:10px; align-items:center; padding:10px 14px; border-top:1px solid var(--line); font-size:14px}
.board .r:first-child{border-top:0}
.board .r.me{background:var(--me)}
.board .r .pos{font-family:"Unbounded",sans-serif; font-weight:800; font-size:14px; color:var(--ink2)}
.board .r .pos.top{color:var(--sun2)}
.board .r .nm b{display:block}
.board .r .nm span{font-size:12px; color:var(--ink2)}
.board .r .pt{font-family:"Unbounded",sans-serif; font-weight:800; font-size:15px}

.dex{display:grid; grid-template-columns:repeat(3,1fr); gap:10px}
.dex .c{background:var(--card); border:1px solid var(--line); border-radius:18px; padding:12px 10px 10px; text-align:center; min-height:118px; display:flex; flex-direction:column; align-items:center; gap:4px; position:relative}
.dex .c .e{font-size:34px; line-height:1}
.dex .c .n{font-size:12px; font-weight:700; line-height:1.2}
.dex .c .h{font-size:11px; color:var(--ink2); line-height:1.25}
.dex .c .k{position:absolute; top:8px; right:10px; font-size:11px; font-weight:700}
.dex .c.no .e{filter:grayscale(1) opacity(.35)}
.dex .c.no .n{color:var(--ink2)}
.dex .c.r1{border-color:color-mix(in srgb,var(--r1) 45%,transparent)}
.dex .c.r2{border-color:color-mix(in srgb,var(--r2) 55%,transparent)}
.dex .c.r3{border-color:color-mix(in srgb,var(--r3) 70%,transparent); box-shadow:0 0 0 1px color-mix(in srgb,var(--r3) 40%,transparent)}
.tag{display:inline-block; font-size:11px; font-weight:700; padding:2px 8px; border-radius:20px; border:1px solid currentColor}
.tag.r0{color:var(--r0)} .tag.r1{color:var(--r1)} .tag.r2{color:var(--r2)} .tag.r3{color:var(--r3)}

.log{display:flex; flex-direction:column; gap:8px}
.log .i{display:flex; gap:12px; align-items:center; background:var(--card); border:1px solid var(--line); border-radius:16px; padding:8px 12px 8px 8px}
.log .i.me{background:var(--me)}
.log .i img,.log .i .ph{width:56px; height:56px; border-radius:11px; object-fit:cover; background:var(--line); flex:none; display:grid; place-items:center; font-size:26px}
.log .i .t{flex:1; min-width:0}
.log .i .t b{display:block; font-size:14px; white-space:nowrap; overflow:hidden; text-overflow:ellipsis}
.log .i .t span{font-size:12px; color:var(--ink2)}
.log .i .p{font-family:"Unbounded",sans-serif; font-weight:800; font-size:16px; color:var(--sun2); white-space:nowrap}
.empty{color:var(--ink2); font-size:14px; background:var(--card); border:1px dashed var(--line); border-radius:16px; padding:16px}

.stats{display:grid; grid-template-columns:1fr 1fr; gap:10px}
.stats div{background:var(--card); border:1px solid var(--line); border-radius:16px; padding:12px 14px}
.stats b{display:block; font-family:"Unbounded",sans-serif; font-weight:800; font-size:20px}
.stats span{font-size:12px; color:var(--ink2)}

details{margin-top:26px; color:var(--ink2); font-size:13px}
summary{cursor:pointer; font-weight:700; color:var(--ink)}
details p{margin:10px 0}
details .row{display:flex; gap:8px; flex-wrap:wrap; margin-top:10px; align-items:center}
details button{border:1px solid var(--line); border-radius:12px; padding:8px 12px; font-size:13px; font-weight:700; color:var(--ink)}

.ov{position:fixed; inset:0; background:rgba(10,10,30,.55); display:none; align-items:flex-end; justify-content:center; z-index:9; padding:12px; backdrop-filter:blur(4px)}
.ov.on{display:flex}
.res{width:100%; max-width:480px; background:var(--bg2); color:var(--ink); border-radius:26px; padding:22px 20px 18px; box-shadow:0 30px 80px rgba(0,0,0,.4); animation:up .35s cubic-bezier(.2,.8,.2,1) both}
@keyframes up{from{transform:translateY(40px); opacity:0} to{transform:none; opacity:1}}
.res .head{display:flex; gap:14px; align-items:center}
.res .head .e{font-size:54px; line-height:1}
.res .head .disp{font-size:22px; line-height:1.1}
.res .head .nm{font-size:14px; margin-top:4px; display:flex; gap:8px; align-items:center; flex-wrap:wrap}
.res .pts{margin:16px 0 4px; display:flex; align-items:baseline; gap:8px}
.res .pts .disp{font-size:48px; color:var(--sun2); line-height:1}
.res .pts span{color:var(--ink2); font-size:14px}
.res .br{display:grid; grid-template-columns:1fr auto; gap:4px 12px; font-size:13px; margin-top:10px; padding-top:10px; border-top:1px solid var(--line)}
.res .br span:nth-child(odd){color:var(--ink2)}
.res .br span:nth-child(even){font-weight:700; text-align:right}
.res .pic{display:flex; gap:12px; align-items:center; margin-top:14px}
.res .pic img{width:64px; height:64px; border-radius:12px; object-fit:cover}
.res .pic p{font-size:13px; color:var(--ink2); margin:0}
.res .ok{margin-top:16px; width:100%; padding:14px; border-radius:16px; background:var(--ink); color:var(--bg2); font-weight:700; font-size:16px}
.res.fail .head .disp{color:var(--bad)}
.res p.note{font-size:13px; color:var(--ink2); margin:12px 0 0}
.res .err{color:var(--bad); font-size:13px; margin-top:8px; min-height:18px}

@media (prefers-reduced-motion: reduce){ .rays{animation:none} .sun.wobble,.sun.pop,.sun.shake,.res{animation:none} }
</style>
</head>
<body>
<div class="wrap">
  <div class="top">
    <div>
      <h1>солнцелов</h1>
      <div class="sub" id="sub">лови солнце, копи очки, зимой дороже</div>
    </div>
    <div class="score">
      <div class="n" id="total">0</div>
      <div class="l" id="streak">серия 0 дней</div>
    </div>
  </div>
  <div class="lvl"><span id="lvl">уровень 1</span><div class="bar"><i id="lvlbar" style="width:0%"></i></div><span id="lvlnext"></span></div>
  <div class="status" id="status">подключаюсь…</div>

  <div class="hero">
    <div class="rays"></div>
    <button class="sun" id="catch" aria-label="поймать солнце">
      <div><div class="disp">поймать</div><small id="sunsub">сфотографируй солнце</small></div>
    </button>
    <input type="file" id="file" accept="image/*" capture="environment" aria-hidden="true" tabindex="-1">
  </div>

  <div class="today">
    <div class="m"><div class="disp" id="mult">×1.0</div><span id="multwhy">множитель сегодня</span></div>
    <div class="g">
      <div><b id="daylen">—</b><span>длина дня</span></div>
      <div><b id="rise">—</b><span>восход</span></div>
      <div><b id="set">—</b><span>закат</span></div>
    </div>
    <div class="why" id="why"></div>
    <div class="loc"><span id="locname">считаю для москвы</span><button id="geo">определить где я</button></div>
  </div>

  <section>
    <h2>друзья <span id="pcount"></span></h2>
    <div class="tabs"><button class="on" id="tabw">за неделю</button><button id="taba">за все время</button></div>
    <div class="board" id="board"><div class="empty">загружаю…</div></div>
  </section>

  <section>
    <h2>лента уловов</h2>
    <div class="log" id="feed"></div>
  </section>

  <section>
    <h2>солнцедекс <span id="dexcount">0 из 11</span></h2>
    <div class="dex" id="dex"></div>
  </section>

  <section>
    <h2>мой журнал</h2>
    <div class="log" id="log"></div>
  </section>

  <section>
    <h2>цифры</h2>
    <div class="stats" id="stats"></div>
  </section>

  <details>
    <summary>как считаются очки</summary>
    <p>солнечность кадра от 0 до 100, считается по яркости, контрасту и засветам, ниже 40 солнце убегает</p>
    <p>сезонный множитель от ×1 в июне до ×3 в конце декабря, зависит от длины светового дня в твоей точке</p>
    <p>час после восхода и час перед закатом дают ×1.5, ночью только ×0.5</p>
    <p>редкость солнца: обычное ×1, редкое ×1.3, эпическое ×1.7, легендарное ×2.5</p>
    <p>серия дней подряд прибавляет по 5% за день, максимум 50%, фото старше полутора суток дают 30%</p>
    <p>за неделю считаются очки за последние 7 дней</p>
  </details>

  <details>
    <summary>мой игрок</summary>
    <p>игрок привязан к этому телефону и браузеру, если сменишь телефон, попросишь, и добавим вход по почте</p>
    <div class="row"><input type="text" id="rename" placeholder="новое имя" maxlength="24" style="flex:1"><button id="renbtn">переименовать</button></div>
    <p id="renmsg"></p>
  </details>
</div>

<div class="ov" id="ov"><div class="res" id="res"></div></div>

<script>
(function(){
  var TYPES=[
    {id:'plain',name:'обычное солнце',em:'☀️',rar:0,hint:'любой ясный день'},
    {id:'cloudy',name:'сквозь облака',em:'🌤️',rar:0,hint:'солнце еле пробивается'},
    {id:'lazy',name:'летнее ленивое',em:'🕶️',rar:0,hint:'лето, середина дня'},
    {id:'blind',name:'ослепительное',em:'🔆',rar:1,hint:'солнце прямо в кадре'},
    {id:'dawn',name:'рассветное',em:'🌅',rar:1,hint:'первый час после восхода'},
    {id:'dusk',name:'закатное',em:'🌇',rar:1,hint:'последний час перед закатом'},
    {id:'autumn',name:'осеннее золото',em:'🍂',rar:1,hint:'сентябрь или октябрь, теплый свет'},
    {id:'winter',name:'зимнее',em:'❄️',rar:2,hint:'день короче 8.5 часов'},
    {id:'nova',name:'сверхновая',em:'💥',rar:2,hint:'очень яркий кадр, все горит'},
    {id:'ghost',name:'ноябрьский призрак',em:'👻',rar:3,hint:'солнце в ноябре, его почти не бывает'},
    {id:'solstice',name:'солнцестояние',em:'🎄',rar:3,hint:'с 19 по 23 декабря'}
  ];
  var RAR=['обычное','редкое','эпическое','легендарное'], RARM=[1,1.3,1.7,2.5];
  var byId={}; TYPES.forEach(function(t){byId[t.id]=t});

  var sb=null, me=null, P=null, myLog=[], players=[], week={}, feed=[], tab='w', ready=false;
  var $=function(id){return document.getElementById(id)};
  var sunBtn=$('catch'), fileIn=$('file'), ov=$('ov'), res=$('res');
  function status(t){$('status').textContent=t}

  /* ---------- boot ---------- */
  function boot(){
    if(!window.supabase||!CONFIG.url||CONFIG.url.indexOf('ВСТАВЬ')>=0||CONFIG.key.indexOf('ВСТАВЬ')>=0){
      status('в файле не вставлены url и ключ supabase'); renderToday(); return;
    }
    sb=window.supabase.createClient(CONFIG.url,CONFIG.key);
    sb.auth.getSession().then(function(r){
      if(r.data&&r.data.session) return r.data.session;
      return sb.auth.signInAnonymously().then(function(r2){
        if(r2.error) throw new Error('анонимный вход не включен в supabase: '+r2.error.message);
        return r2.data.session;
      });
    }).then(function(s){
      me=s.user.id;
      return sb.from('players').select('*').eq('id',me).maybeSingle();
    }).then(function(r){
      if(r.error) throw new Error(r.error.message);
      if(r.data){P=r.data; start()} else askName();
    }).catch(function(e){status('ошибка: '+(e.message||e)); renderToday()});
  }
  function askName(){
    renderToday();
    res.className='res';
    res.innerHTML='<div class="head"><div class="e">☀️</div><div><div class="disp">как тебя зовут?</div><div class="nm">имя увидят друзья в таблице</div></div></div>'+
      '<div style="margin-top:14px"><input type="text" id="nameIn" placeholder="имя" maxlength="24" autocomplete="off"></div><div class="err" id="nameErr"></div>'+
      '<button class="ok" id="nameOk">в игру</button>';
    ov.classList.add('on');
    var inp=$('nameIn'); setTimeout(function(){inp.focus()},50);
    function go(){
      var n=inp.value.trim(); if(n.length<1){$('nameErr').textContent='введи имя'; return}
      $('nameOk').disabled=true;
      sb.from('players').insert({id:me,name:n}).select().single().then(function(r){
        if(r.error){ $('nameOk').disabled=false; $('nameErr').textContent=r.error.code==='23505'?'такое имя уже занято, добавь фамилию или букву':'не вышло: '+r.error.message; return }
        P=r.data; ov.classList.remove('on'); start();
      });
    }
    $('nameOk').onclick=go; inp.onkeydown=function(e){if(e.key==='Enter') go()};
  }
  function start(){
    ready=true; $('sub').textContent='привет, '+P.name;
    status('онлайн');
    render(); loadMine(); loadFriends();
    var t=null; function later(){clearTimeout(t); t=setTimeout(loadFriends,800)}
    sb.channel('game')
      .on('postgres_changes',{event:'INSERT',schema:'public',table:'catches'},later)
      .on('postgres_changes',{event:'UPDATE',schema:'public',table:'players'},later)
      .on('postgres_changes',{event:'INSERT',schema:'public',table:'players'},later)
      .subscribe();
    setInterval(loadFriends,120000);
  }

  /* ---------- data ---------- */
  function loadMine(){
    sb.from('catches').select('id,type,pts,score,taken_at,created_at,thumb').eq('player_id',me).order('created_at',{ascending:false}).limit(40)
      .then(function(r){ if(!r.error){myLog=r.data||[]; renderMine()} });
  }
  function loadFriends(){
    var since=new Date(Date.now()-7*86400000).toISOString();
    Promise.all([
      sb.from('players').select('id,name,total,streak,best,count,caught').order('total',{ascending:false}),
      sb.from('catches').select('player_id,pts').gte('created_at',since),
      sb.from('catches').select('id,type,pts,score,taken_at,created_at,thumb,player_id,players(name)').order('created_at',{ascending:false}).limit(30)
    ]).then(function(rs){
      if(!rs[0].error) players=rs[0].data||[];
      if(!rs[1].error){week={}; (rs[1].data||[]).forEach(function(c){week[c.player_id]=(week[c.player_id]||0)+c.pts})}
      if(!rs[2].error) feed=rs[2].data||[];
      renderFriends();
    }).catch(function(){});
  }
  function savePlayer(fields){
    return sb.from('players').update(fields).eq('id',me).then(function(r){ if(r.error) status('не сохранилось: '+r.error.message); else status('онлайн'); });
  }

  /* ---------- sun math ---------- */
  var RAD=Math.PI/180;
  function sunTimes(lat,lon,d){
    var start=new Date(d.getFullYear(),0,0);
    var N=Math.floor((d-start)/86400000);
    var B=360/365*(N-81)*RAD;
    var eot=9.87*Math.sin(2*B)-7.53*Math.cos(B)-1.5*Math.sin(B);
    var decl=23.44*RAD*Math.sin(B);
    var tz=-d.getTimezoneOffset()/60;
    var noon=12+tz-lon/15-eot/60;
    var cosH=(Math.sin(-0.833*RAD)-Math.sin(lat*RAD)*Math.sin(decl))/(Math.cos(lat*RAD)*Math.cos(decl));
    cosH=Math.max(-1,Math.min(1,cosH));
    var H=Math.acos(cosH)/RAD/15;
    return {daylen:2*H,rise:noon-H,set:noon+H,noon:noon};
  }
  function dayLenAt(lat,decl){
    var c=(Math.sin(-0.833*RAD)-Math.sin(lat*RAD)*Math.sin(decl))/(Math.cos(lat*RAD)*Math.cos(decl));
    c=Math.max(-1,Math.min(1,c)); return 2*Math.acos(c)/RAD/15;
  }
  function seasonMult(lat,daylen){
    var a=dayLenAt(lat,23.44*RAD), b=dayLenAt(lat,-23.44*RAD), mx=Math.max(a,b), mn=Math.min(a,b);
    if(mx-mn<0.5) return 1;
    var t=Math.max(0,Math.min(1,(mx-daylen)/(mx-mn)));
    return Math.round((1+2*Math.pow(t,1.5))*10)/10;
  }
  function hhmm(h){h=((h%24)+24)%24; var m=Math.round(h*60); return Math.floor(m/60)+':'+('0'+(m%60)).slice(-2)}
  function hlen(h){var m=Math.round(h*60); return Math.floor(m/60)+'ч '+('0'+(m%60)).slice(-2)+'м'}
  function dayKey(d){return d.getFullYear()+'-'+(d.getMonth()+1)+'-'+d.getDate()}
  function loc(){return P?{lat:P.lat,lon:P.lon,name:P.loc_name}:{lat:55.75,lon:37.62,name:'москвы'}}

  /* ---------- photo ---------- */
  function analyze(img){
    var W=img.naturalWidth||img.width, H=img.naturalHeight||img.height;
    var k=Math.min(1,220/Math.max(W,H));
    var w=Math.max(1,Math.round(W*k)), h=Math.max(1,Math.round(H*k));
    var c=document.createElement('canvas'); c.width=w; c.height=h;
    var x=c.getContext('2d'); x.drawImage(img,0,0,w,h);
    var d=x.getImageData(0,0,w,h).data, n=w*h;
    var sum=0,sq=0,hl=0,warm=0,sky=0;
    for(var i=0;i<d.length;i+=4){
      var r=d[i],g=d[i+1],b=d[i+2], l=0.2126*r+0.7152*g+0.0722*b;
      sum+=l; sq+=l*l; if(l>235) hl++; warm+=(r-b); if(b>r+18&&b>120&&l>100) sky++;
    }
    var mean=sum/n, std=Math.sqrt(Math.max(0,sq/n-mean*mean));
    var bright=clamp((mean-60)/120), contrast=clamp((std-25)/50), hlf=hl/n, hln=clamp(hlf/0.03), skyf=sky/n;
    return {score:Math.round(100*(0.35*bright+0.30*contrast+0.25*hln+0.10*clamp(skyf/0.25))),hl:hlf,warm:warm/n};
  }
  function clamp(v){return Math.max(0,Math.min(1,v))}
  function classify(a,ctx){
    var m=ctx.month;
    if(m===11&&ctx.day>=19&&ctx.day<=23) return 'solstice';
    if(m===10) return 'ghost';
    if(ctx.daylen<8.5) return 'winter';
    if(a.hl>0.12&&a.score>=85) return 'nova';
    if(ctx.gold==='dawn') return 'dawn';
    if(ctx.gold==='dusk') return 'dusk';
    if((m===8||m===9)&&a.warm>12) return 'autumn';
    if(a.hl>0.05&&a.score>=70) return 'blind';
    if(a.score<55) return 'cloudy';
    if(m>=5&&m<=7&&ctx.mid) return 'lazy';
    return 'plain';
  }
  function makeThumb(img){
    try{var W=img.naturalWidth||img.width,H=img.naturalHeight||img.height,s=Math.min(W,H),c=document.createElement('canvas'); c.width=96; c.height=96;
      c.getContext('2d').drawImage(img,(W-s)/2,(H-s)/2,s,s,0,0,96,96); return c.toDataURL('image/jpeg',0.6)}catch(e){return ''}
  }

  /* ---------- catch ---------- */
  sunBtn.addEventListener('click',function(){
    if(!ready){toast('сначала нужно подключиться, смотри строку под уровнем'); return}
    fileIn.value=''; fileIn.click();
  });
  fileIn.addEventListener('change',function(){
    var f=fileIn.files&&fileIn.files[0]; if(!f) return;
    var url=URL.createObjectURL(f), img=new Image();
    img.onload=function(){
      sunBtn.classList.remove('pop','shake'); void sunBtn.offsetWidth; sunBtn.classList.add('wobble');
      var when=new Date(); if(f.lastModified&&f.lastModified<Date.now()+60000&&f.lastModified>Date.now()-1000*3600*24*365*5) when=new Date(f.lastModified);
      var a=analyze(img), thumb=makeThumb(img);
      setTimeout(function(){sunBtn.classList.remove('wobble'); finish(a,when,thumb); URL.revokeObjectURL(url)},1400);
    };
    img.onerror=function(){toast('не смог прочитать фото, попробуй другое'); URL.revokeObjectURL(url)};
    img.src=url;
  });
  function finish(a,when,thumb){
    var L=loc(), st=sunTimes(L.lat,L.lon,when), hr=when.getHours()+when.getMinutes()/60;
    var gold='',mid=false,tf=1,tlabel='обычный день';
    if(hr<st.rise||hr>st.set){tf=0.5; tlabel='солнце за горизонтом'}
    else if(hr-st.rise<=1){gold='dawn'; tf=1.5; tlabel='золотой час, утро'}
    else if(st.set-hr<=1){gold='dusk'; tf=1.5; tlabel='золотой час, вечер'}
    else if(Math.abs(hr-st.noon)<2) mid=true;
    var sm=seasonMult(L.lat,st.daylen);
    if(a.score<40){sunBtn.classList.add('shake'); showRes({fail:true,score:a.score,thumb:thumb}); return}
    var id=classify(a,{month:when.getMonth(),day:when.getDate(),daylen:st.daylen,gold:gold,mid:mid}), t=byId[id];
    var now=new Date(), today=dayKey(now), yest=dayKey(new Date(now.getTime()-86400000));
    if(P.last_day!==today){P.streak=(P.last_day===yest)?P.streak+1:1; P.last_day=today}
    var sbn=Math.min(0.5,0.05*(P.streak-1));
    var stale=(Date.now()-when.getTime())>1000*3600*36, ff=stale?0.3:1;
    var pts=Math.max(1,Math.round(a.score*sm*tf*RARM[t.rar]*(1+sbn)*ff));
    var caught=Object.assign({},P.caught||{}); var isNew=!caught[id]; caught[id]=(caught[id]||0)+1;
    P.caught=caught; P.total+=pts; P.count=(P.count||0)+1; if(pts>P.best) P.best=pts;
    var entry={player_id:me,type:id,pts:pts,score:a.score,taken_at:when.toISOString(),thumb:thumb};
    myLog.unshift(Object.assign({created_at:new Date().toISOString()},entry)); if(myLog.length>40) myLog.length=40;
    render(); renderMine();
    sunBtn.classList.add('pop');
    showRes({type:t,pts:pts,score:a.score,sm:sm,tf:tf,tlabel:tlabel,sb:sbn,stale:stale,thumb:thumb,isNew:isNew});
    savePlayer({total:P.total,streak:P.streak,last_day:P.last_day,best:P.best,count:P.count,caught:P.caught,updated_at:new Date().toISOString()})
      .then(function(){return sb.from('catches').insert(entry)})
      .then(function(r){ if(r&&r.error) status('улов не записался: '+r.error.message); loadFriends() });
  }

  /* ---------- result ---------- */
  function showRes(r){
    var h='';
    if(r.fail){
      res.className='res fail';
      h+='<div class="head"><div class="e">🌫️</div><div><div class="disp">сбежало</div><div class="nm">солнечность '+r.score+' из 100, нужно хотя бы 40</div></div></div>';
      h+='<div class="pic">'+(r.thumb?'<img src="'+r.thumb+'" alt="">':'')+'<p>это не солнце, это тучи или лампа, попробуй кадр посветлее и с тенями</p></div>';
    }else{
      res.className='res'; var t=r.type;
      h+='<div class="head"><div class="e">'+t.em+'</div><div><div class="disp">'+(r.isNew?'новое солнце!':'поймано!')+'</div><div class="nm">'+t.name+' <span class="tag r'+t.rar+'">'+RAR[t.rar]+'</span></div></div></div>';
      h+='<div class="pts"><div class="disp">+'+r.pts+'</div><span>очков</span></div><div class="br">';
      h+='<span>солнечность</span><span>'+r.score+'</span><span>сезон</span><span>×'+r.sm+'</span><span>'+r.tlabel+'</span><span>×'+r.tf+'</span><span>редкость</span><span>×'+RARM[t.rar]+'</span>';
      if(r.sb>0) h+='<span>серия '+P.streak+' дн</span><span>+'+Math.round(r.sb*100)+'%</span>';
      if(r.stale) h+='<span>фото старше 36 часов</span><span>×0.3</span>';
      h+='</div>';
      if(r.thumb) h+='<div class="pic"><img src="'+r.thumb+'" alt=""><p>'+t.hint+'</p></div>';
    }
    h+='<button class="ok" id="okbtn">ок</button>';
    res.innerHTML=h; ov.classList.add('on'); $('okbtn').onclick=function(){ov.classList.remove('on')};
  }
  function toast(msg){res.className='res fail'; res.innerHTML='<div class="head"><div class="e">🙈</div><div><div class="disp">не вышло</div><div class="nm">'+msg+'</div></div></div><button class="ok" id="okbtn">ок</button>'; ov.classList.add('on'); $('okbtn').onclick=function(){ov.classList.remove('on')}}
  ov.addEventListener('click',function(e){if(e.target===ov&&P) ov.classList.remove('on')});

  /* ---------- render ---------- */
  function level(total){var n=1; while(total>=150*n*n) n++; return n}
  function esc(s){return String(s==null?'':s).replace(/[&<>"]/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]})}
  function renderToday(){
    var L=loc(), now=new Date(), st=sunTimes(L.lat,L.lon,now), sm=seasonMult(L.lat,st.daylen);
    $('mult').textContent='×'+sm; $('daylen').textContent=hlen(st.daylen); $('rise').textContent=hhmm(st.rise); $('set').textContent=hhmm(st.set);
    var hr=now.getHours()+now.getMinutes()/60, why='', m=now.getMonth();
    if(hr<st.rise||hr>st.set){why='сейчас солнце за горизонтом, ловить можно, но только ×0.5'; sunBtn.classList.add('night'); $('sunsub').textContent='солнце спит, но ты можешь попробовать'}
    else{sunBtn.classList.remove('night');
      if(hr-st.rise<=1){why='золотой час после восхода, сейчас ×1.5 сверху'; $('sunsub').textContent='золотой час, беги на улицу'}
      else if(st.set-hr<=1){why='золотой час перед закатом, сейчас ×1.5 сверху, еще '+Math.round((st.set-hr)*60)+' мин'; $('sunsub').textContent='золотой час, беги на улицу'}
      else{why='до заката '+hlen(st.set-hr)+', перед ним будет золотой час'; $('sunsub').textContent='сфотографируй солнце'}
    }
    if(m===10) why+='. ноябрь, любое солнце легендарное';
    else if(m===11&&now.getDate()>=19&&now.getDate()<=23) why+='. солнцестояние, самое дорогое солнце в году';
    else if(st.daylen<8.5) why+='. день короткий, солнце эпическое';
    $('why').textContent=why;
    $('multwhy').textContent=sm>=2.5?'зима, дорого':sm>=1.8?'дни короткие, выгодно':sm>=1.3?'межсезонье':'лето, дешево';
    $('locname').textContent='считаю для '+L.name;
  }
  function render(){
    renderToday();
    if(!P) return;
    $('total').textContent=P.total;
    $('streak').textContent='серия '+P.streak+' '+plural(P.streak,'день','дня','дней');
    var Lv=level(P.total), lo=150*(Lv-1)*(Lv-1), hi=150*Lv*Lv;
    $('lvl').textContent='уровень '+Lv; $('lvlbar').style.width=Math.round(100*(P.total-lo)/(hi-lo))+'%'; $('lvlnext').textContent='до '+(Lv+1)+' еще '+(hi-P.total);
    var n=0, dh=''; var caught=P.caught||{};
    TYPES.forEach(function(t){var c=caught[t.id]||0; if(c) n++;
      dh+='<div class="c r'+t.rar+(c?'':' no')+'"><div class="e">'+t.em+'</div><div class="n">'+t.name+'</div><div class="h">'+(c?RAR[t.rar]:t.hint)+'</div>'+(c?'<div class="k" style="color:var(--r'+t.rar+')">×'+c+'</div>':'')+'</div>'});
    $('dex').innerHTML=dh; $('dexcount').textContent=n+' из '+TYPES.length;
  }
  function item(e,name,mine){
    var t=byId[e.type]||TYPES[0], d=new Date(e.taken_at||e.created_at);
    return '<div class="i'+(mine?' me':'')+'">'+(e.thumb?'<img src="'+e.thumb+'" alt="">':'<div class="ph">'+t.em+'</div>')+
      '<div class="t"><b>'+(name?esc(name)+': ':'')+t.em+' '+t.name+'</b><span>'+fmt(d)+', солнечность '+e.score+'</span></div><div class="p">+'+e.pts+'</div></div>';
  }
  function renderMine(){
    var lh=''; if(!myLog.length) lh='<div class="empty">пока пусто, нажми на солнце и сфотографируй его</div>';
    myLog.forEach(function(e){lh+=item(e,'',false)}); $('log').innerHTML=lh;
    var bestT=null; myLog.forEach(function(e){if(!bestT||e.pts>bestT.pts) bestT=e});
    var pos=0; players.forEach(function(p,i){if(p.id===me) pos=i+1});
    $('stats').innerHTML='<div><b>'+(P.count||0)+'</b><span>солнц поймано</span></div>'+
      '<div><b>'+(pos?pos+' из '+players.length:'—')+'</b><span>место за все время</span></div>'+
      '<div><b>'+P.best+'</b><span>лучший улов</span></div>'+
      '<div><b>'+(bestT?byId[bestT.type].em:'—')+'</b><span>'+(bestT?byId[bestT.type].name:'самое дорогое солнце')+'</span></div>';
  }
  function renderFriends(){
    $('pcount').textContent=players.length+' '+plural(players.length,'игрок','игрока','игроков');
    var rows=players.map(function(p){return {id:p.id,name:p.name,streak:p.streak,count:p.count,pts:tab==='w'?(week[p.id]||0):p.total}});
    rows.sort(function(a,b){return b.pts-a.pts});
    var h=''; if(!rows.length) h='<div class="empty">пока никого, зови друзей</div>';
    rows.forEach(function(r,i){
      var caughtN=0; var pl=players.filter(function(p){return p.id===r.id})[0]; if(pl&&pl.caught) caughtN=Object.keys(pl.caught).length;
      h+='<div class="r'+(r.id===me?' me':'')+'"><div class="pos'+(i<3&&r.pts>0?' top':'')+'">'+(i+1)+'</div><div class="nm"><b>'+esc(r.name)+'</b><span>серия '+r.streak+', в декcе '+caughtN+' из '+TYPES.length+'</span></div><div class="pt">'+r.pts+'</div></div>';
    });
    $('board').innerHTML=h;
    var fh=''; if(!feed.length) fh='<div class="empty">никто еще ничего не поймал</div>';
    feed.forEach(function(e){fh+=item(e,e.players?e.players.name:'?',e.player_id===me)}); $('feed').innerHTML=fh;
    if(P) renderMine();
  }
  $('tabw').onclick=function(){tab='w'; this.classList.add('on'); $('taba').classList.remove('on'); renderFriends()};
  $('taba').onclick=function(){tab='a'; this.classList.add('on'); $('tabw').classList.remove('on'); renderFriends()};
  function fmt(d){var M=['янв','фев','мар','апр','мая','июн','июл','авг','сен','окт','ноя','дек']; return d.getDate()+' '+M[d.getMonth()]+' '+hhmm(d.getHours()+d.getMinutes()/60)}
  function plural(n,a,b,c){n=Math.abs(n)%100; var n1=n%10; if(n>10&&n<20) return c; if(n1>1&&n1<5) return b; if(n1===1) return a; return c}

  /* ---------- location, rename ---------- */
  $('geo').addEventListener('click',function(){
    var b=this; if(!P){b.textContent='сначала войди'; return}
    if(!navigator.geolocation){b.textContent='геолокации нет'; return}
    b.textContent='ищу…';
    navigator.geolocation.getCurrentPosition(function(p){
      P.lat=Math.round(p.coords.latitude*100)/100; P.lon=Math.round(p.coords.longitude*100)/100; P.loc_name='твоей точки '+Math.round(P.lat)+'° '+Math.round(P.lon)+'°';
      savePlayer({lat:P.lat,lon:P.lon,loc_name:P.loc_name}); render(); b.textContent='обновить';
    },function(){b.textContent='не дали доступ, оставляю как было'},{timeout:8000,maximumAge:600000});
  });
  $('renbtn').addEventListener('click',function(){
    var n=$('rename').value.trim(); if(!P||!n) return;
    sb.from('players').update({name:n}).eq('id',me).then(function(r){
      if(r.error){$('renmsg').textContent=r.error.code==='23505'?'имя занято':'не вышло: '+r.error.message; return}
      P.name=n; $('sub').textContent='привет, '+n; $('renmsg').textContent='теперь ты '+n; $('rename').value=''; loadFriends();
    });
  });

  boot();
  setInterval(renderToday,60000);
})();
</script>
</body>
</html>
